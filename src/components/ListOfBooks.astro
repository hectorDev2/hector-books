---
import BookItem from "./BookItem.astro";

import { booksDB } from "../data/books";
---

<ul class="my-10 grid grid-cols-2 md:grid-cols-3 gap-3">
  {booksDB.map((book) => <BookItem title={book.title} img={book.img} />)}
</ul>
